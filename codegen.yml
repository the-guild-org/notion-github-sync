schema:
  - https://api.github.com/graphql:
      headers:
        Authorization: Bearer ${GH_BOT_TOKEN}
documents: "src/**/*.ts"
generates:
  src/types.ts:
    config:
      scalars:
        URI: string
    plugins:
      - "typescript"
      - "typescript-operations"
